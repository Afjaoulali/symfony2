<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
		<link rel="stylesheet" href="{{asset('css/style.css')}}">
		<script src="https://kit.fontawesome.com/f668cda814.js" crossorigin="anonymous"></script>
		<title>Agence de location</title>
	</head>
	<body>

		<header>
			{% block header %}

				<nav class="navbar navbar-expand-lg navbar-light bg-light">
					<a class="navbar-brand" href="{{ path('app_accueil') }}">Accueil</a>
					<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>

					<div class="collapse navbar-collapse" id="navbarSupportedContent">
						<ul class="navbar-nav mr-auto">
							<li class="nav-item active">
								{#le liennnn de connexion #}
								
								<a class="nav-link" href="{{ path('app_login') }}">Connexion/Deconnexion</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ path('list_vehicule') }}">Voiture</a>
							</li>

                            
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
									Contact
								</a>
								<div class="dropdown-menu" aria-labelledby="navbarDropdown">
									<a class="dropdown-item" href="#">00 33 01 00 00 00 00</a>
									<a class="dropdown-item" href="#">agence@gmail.com</a>
								</div>
							</li>
						</ul>

						    <div class="d-flex">
                            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                <a class="btn btn-outline-danger me-2" href="{{ path('app_logout') }}">Déconnexion</a>
                            {% else %}
							<a class="nav-link" href="{{ path('app_login') }}">Connexion/Inscription</a>
                            {% endif %}

                        </div>

						<form action="{{ path('front_search') }}" method="get" class="form-inline my-2 my-lg-0 " id="form-inline">
						<input class="mr-sm-2" type="search" placeholder="Search" aria-label="Search" type="search" name="term" id="term">
						<button class="btn btn-outline-success my-2 my-sm-0" type="submit" value="Chercher">Chercher</button>
					</form>
				</div>
			</nav>
		</body>
	</html>
</header>{% endblock %}<main>
{% block main %}{% endblock %}</main></body></html>

