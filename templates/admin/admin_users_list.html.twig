{% extends "admin/base.html.twig" %}

{% block stylesheets %}
	<link rel="stylesheet" href="">
{% endblock %}

{% block main %}
	<div class="container">
		<a href="{{ path('create_admin_user')}}">Ajouter un membre</a>
	</div>

	<div class="container">

		<table class="table">
			<thead>
				<tr>
                <th>Id membre</th>
                <th>Pseudo</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Email</th>
                <th>Civilité</th>
                <th>Statut</th>
                <th>Date d'enregistrement</th>
                <th>Actions</th>  
				</tr>
			</thead>
			{% for user in users %}
				<tbody>
					<tr>
						<td scope="row">{{ user.id }}</td>
						<td scope="row">
							<a href="{{ path('admin_user_show', {'id' : user.id}) }}">{{ user.pseudo }}</td>
						</td>
						<td scope="row">{{ user.nom }}</td>
						<td scope="row">{{ user.prenom }}</td>
						<td scope="row">{{ user.email }}</td>
						<td scope="row">{{ user.civilite }}</td>
						<td scope="row">{{ user.statut }}</td>
						<td scope="row">{{ user.dateEnregistrement | date('d-m-Y h:m') }}</td>
						<td>
							<a href="{{ path('admin_user_update', {'id' : user.id}) }}">
								<i class="fa-solid fa-pen"></i>
							</a>
							<a href=" {{ path('delete_admin_user', {'id' : user.id}) }}">
								<i class="fa-solid fa-trash"></i>
							</a>
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>

		</div>


	{% endblock %}
